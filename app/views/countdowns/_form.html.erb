<%= form_for(@countdown) do |f| %>
  <% if @countdown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@countdown.errors.count, "error") %> prohibited this countdown from being saved:</h2>

      <ul>
      <% @countdown.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><span class="optional"> (optional)</span><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :link %><span class="optional"> (optional)</span><br>
    <%= f.text_field :link %>
  </div>
  <div class="field date_select">
    <%= f.label :countdown_date %><br>
    <%= f.datetime_select :countdown_date%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>